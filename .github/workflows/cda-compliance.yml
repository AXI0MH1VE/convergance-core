name: CDA Compliance
on: [push, workflow_dispatch]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: test -f .github/CONSTITUTION.md && test -f .github/COMPLIANCE.md && grep -q CDA README.md && echo "✅ Compliant"